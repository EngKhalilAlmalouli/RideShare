import 'package:flutter/material.dart';

class HomeScreenTransport extends StatelessWidget {
  const HomeScreenTransport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}